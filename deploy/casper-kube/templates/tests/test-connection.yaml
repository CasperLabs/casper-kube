apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "casper-kube.fullname" . }}-test-connection"
  labels:
    {{- include "casper-kube.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "casper-kube.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
